name: Build JAR with Maven

on:
  push:
    branches: [ "main" ]
  # pull_request:
  #   branches: [ "staging" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Checkout code
    - name: Checkout code
      uses: actions/checkout@v4

    # Step 2: Set up JDK 21
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    # Step 3: Build with Maven (this will create the .jar file)
    - name: Build with Maven
      run: mvn clean package --file ./pom.xml  # Use the correct path to pom.xml

    # Optional: Upload artifact if needed
    - name: Upload JAR artifact
      uses: actions/upload-artifact@v3
      with:
        name: my-app-jar1
        path: target/*.jar
